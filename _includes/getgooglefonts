{% if page.fonts %}<link href="https://fonts.googleapis.com/css?family={% for font in page.fonts %}{% unless forloop.first %}|{% endunless %}{% if font.first %}{{ font.first[0] | split: " " | join: "+" }}:{{ font.first[1] | join: "," }}{% else %}{{ font | split: " " | join: "+" }}{% endif %}{% endfor %}" rel="stylesheet" type="text/css">{% endif %}